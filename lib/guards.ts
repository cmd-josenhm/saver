const PRIVATE_OR_INTERNAL=new Set(["localhost","127.0.0.1","0.0.0.0","::1","169.254.169.254"]);export function safeExternalHttps(raw:string){try{const u=new URL(raw);if(u.protocol!=="https:")return false;const host=u.hostname.toLowerCase().replace(/^www\\./,"");if(PRIVATE_OR_INTERNAL.has(host))return false;if(host.endsWith(".local")||host.endsWith(".internal"))return false;return true}catch{return false}}