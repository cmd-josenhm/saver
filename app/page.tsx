"use client";
import {useState} from "react";
export default function Home(){const[url,setUrl]=useState("");const[busy,setBusy]=useState(false);const[msg,setMsg]=useState("");async function submit(e:React.FormEvent){e.preventDefault();setBusy(true);setMsg("");try{const r=await fetch("/api/jobs",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({url})});const d=await r.json();setMsg(r.ok?"Opération lancée.":d.error||"Erreur.");}catch{setMsg("Erreur réseau.")}finally{setBusy(false)}}return <main><header><b>JOSE<span>WORLD</span> / SAVER</b><a href="https://jose-nahounme.netlify.app/" target="_blank" rel="noreferrer">Visiter mon portfolio ↗</a></header><section><small>INSTAGRAM • TIKTOK</small><h1>Téléchargez vos contenus publics, simplement.</h1><p>Collez une URL publique compatible. Aucun identifiant n’est demandé. Les espaces privés sont refusés.</p><form onSubmit={submit}><input value={url} onChange={e=>setUrl(e.target.value)} placeholder="https://www.instagram.com/... ou https://www.tiktok.com/@..." required/><button disabled={busy}>{busy?"Analyse…":"Télécharger"}</button></form>{msg&&<div role="status">{msg}</div>}<p className="legal">50 fichiers maximum par ZIP • 5 ZIP recommandés par opération. Utilisez Saver uniquement pour les contenus que vous êtes autorisé à télécharger. Aucun contournement d’authentification, CAPTCHA, DRM ou contrôle d’accès.</p></section></main>}